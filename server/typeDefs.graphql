type Member{
    code: Int!
    token: String
}

type Query{
    idCheck(id:String!): Boolean
    nameCheck(name:String!): Boolean
    login(id:String!, pw:String!): Member
}

type Mutation{
    register(name:String!, id:String!, pw:String!): Member
}